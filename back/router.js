const express = require("express");
const bodyParser = require("body-parser");
const router = express.Router();
const sqlite3 = require('sqlite3').verbose();
const quizz = require('./db_function/quizz.js');
const score = require('./db_function/score.js');
const vote = require('./db_function/vote.js');
console.log(quizz);
const db = new sqlite3.Database('./db/quizz');

module.exports = router;

/*
* Middleware
*/

router
    .use(bodyParser.json())
    .use(bodyParser.urlencoded({
    extended: true
}));

router.get("/", (req, res) => {
  res.json("Hello world!");
});

 Quizz 

// Quizz List //
router.get("/quizzes", (req, res) => {
  quizz.list(req, res);
});

// Particular Quizz Informations //
router.get('/quizz_info/:id', (req, res) => {
  quizz.info(req, res);
});

// Quizz Questions //
router.get('/quizz/:id', (req, res) => {
  quizz.questions(req, res);
});

// Quizz Answers //
router.get('/answers/:id', (req, res) => {
  quizz.answers(req, res);
});

// Create Quizz //
router.post('/quizz',(req, res) => {
  quizz.create(req, res);
});

// Create question //
router.post('/question',(req, res) => {
    quizz.create_question(req, res);
});

// Create answers //
router.post('/answers',(req, res) => {
    quizz.create_answers(req, res);
});

// delete questions //
router.delete('/question/:id',(req, res) => {
    quizz.drop_question(req, res);
});

// delete answers //
router.delete('/answers/:id',(req, res) => {
    quizz.drop_answers(req, res);
});

// Delete Quizz //
router.delete('/quizz/:id', (req, res) => {
    quizz.drop(req, res);
});

 Scores 

// All scores //
router.get('/scores', (req, res) => {
  score.all(req, res);
});

// Scores of a particular quizz //
router.get('/scores/:id', (req, res) => {
  score.quizz(req, res);
});

// Best scores of a particular quizz //
router.get('/highscores/:id', (req, res) => {
  score.quizzLeaderboard(req, res);
});

// Scores of a particular user //
router.get('/userscores/:id', (req, res) => {
  score.user(req, res);
});

// Score of a particular user for a giver quizz//
router.get('/score/:user_id/:quizz_id', (req, res) => {
  score.userQuizz(req, res);
});

// Add Score //
router.post('/score', (req, res) => {

});

 Vote 

// all vote //
router.get('/votes', (req, res) => {
  vote.all(req, res);
});

// Add vote //
router.post('/vote', (req, res) => {
  vote.add(req, res);
});

 Users 





router.post('/upload', (req, res) => {
    if (!req.files)
        return res.status(500).send({ msg: "file is not found" })

    // accessing the file
    const myFile = req.files.file;


    //  mv() method places the file inside public directory
    myFile.mv(`${__dirname}/data/img/${myFile.name}`, function (err) {
        if (err) {
            console.log(err)
            return res.status(500).send({ msg: "Error occured" });
        }
        // returing the response with file path and name
        console.log(myFile.name);
        return res.send({name: myFile.name});
    });
});



/*
* 400 Errors handling
*/

router.use((req, res) => {
    res.status(400);
    res.json({
       error: "Bad request"
    });
});
